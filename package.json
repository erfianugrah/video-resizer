{
	"name": "video-resizer",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc --noEmit",
		"build:debug-ui": "cd debug-ui && npm run build",
		"build:all": "npm run build && npm run build:debug-ui",
		"clean:debug-ui": "cd debug-ui && npm run clean",
		"cf-typegen": "wrangler types",
		"config": "node tools/config-wrapper.js",
		"deploy": "npm run build:all && wrangler deploy",
		"deploy:prod": "npm run build:all && wrangler deploy --env production",
		"deploy:dev": "npm run build:all && wrangler deploy --env development",
		"deploy:staging": "npm run build:all && wrangler deploy --env staging",
		"deploy:full": "npm run typecheck && npm run lint && npm run build:all && wrangler deploy",
		"deploy:full:prod": "npm run typecheck && npm run lint && npm run build:all && wrangler deploy --env production",
		"deploy:full:dev": "npm run typecheck && npm run lint && npm run build:all && wrangler deploy --env development",
		"deploy:full:staging": "npm run typecheck && npm run lint && npm run build:all && wrangler deploy --env staging",
		"dev": "wrangler dev",
		"dev:debug-ui": "cd debug-ui && npm run dev",
		"dev:with-ui": "npm run build:debug-ui && wrangler dev",
		"format": "prettier --write .",
		"lint": "eslint .",
		"lint:debug-ui": "eslint debug-ui/src/",
		"lint:fix": "eslint . --fix",
		"lint:src": "eslint src/",
		"start": "wrangler dev",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage",
		"test:domain": "vitest run test/domain",
		"test:edge-cases": "vitest run test/edge-cases",
		"test:integration": "vitest run test/integration",
		"test:services": "vitest run test/services",
		"test:utils": "vitest run test/utils",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"typecheck:debug-ui": "cd debug-ui && npm run check"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.0",
		"@types/node": "^20.11.24",
		"@types/uuid": "^10.0.0",
		"@typescript-eslint/eslint-plugin": "^7.4.0",
		"@typescript-eslint/parser": "^7.4.0",
		"@vitest/coverage-v8": "^3.0.8",
		"eslint": "^8.57.0",
		"eslint-config-prettier": "^9.1.0",
		"eslint-plugin-prettier": "^5.1.3",
		"prettier": "^3.2.5",
		"typescript": "^5.5.2",
		"typescript-eslint": "^8.26.1",
		"vitest": "^3.0.8",
		"wrangler": "^4.20.5"
	},
	"dependencies": {
		"aws4fetch": "^1.0.20",
		"commander": "^13.1.0",
		"node-fetch": "^3.3.2",
		"pino": "^9.6.0",
		"pino-pretty": "^13.0.0",
		"uuid": "^11.1.0",
		"zod": "^3.24.2"
	}
}
